# --- Web framework -----------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1

# --- HTTP / IBM Quantum REST --------------------------------------------------
httpx==0.28.1

# --- Typing / settings --------------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# --- In-process cache + optional history --------------------------------------
cachetools==5.5.0
aiosqlite==0.20.0

# --- Tests --------------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
respx==0.22.0
